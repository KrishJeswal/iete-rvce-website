# IETE-RVCE Website (Frontend)

A standalone frontend built with **Vite + React + TypeScript + Tailwind CSS + shadcn/ui**.

## Run locally
```bash
npm install
npm run dev
```

## Build
```bash
npm run build
npm run preview
```

## Notes
- Standard Vite/React project (no vendor-specific tooling required).
